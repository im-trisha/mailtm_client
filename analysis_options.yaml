include: package:lints/core.yaml

analyzer:
  exclude: [build/**]
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    - cancel_subscriptions
    - package_api_docs
    - public_member_api_docs
    # - comment_references Maybe should be understood better?